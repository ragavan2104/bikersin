# Firebase Configuration
FIREBASE_PROJECT_ID="bikers-2f015"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxx@bikers-2f015.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_FIREBASE_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# JWT Secret (Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET="a1b2c3d4e5f6789012345678901234567890abcdef1234567890abcdef123456789012345678901234567890abcdef123456"

# Node Environment
NODE_ENV=production

# CORS Origins (your frontend domains)
CORS_ORIGINS="https://bikers.ragav.dev,https://mybikers.ragav.dev,http://localhost:3000,http://localhost:3001"

# Port (for local development, Vercel handles this automatically)
PORT=5000

# Rate Limiting (Optional - for Upstash Redis in production)
# UPSTASH_REDIS_REST_URL="your-upstash-redis-url"
# UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-token"

# 📝 FIREBASE SETUP:
# 1. Go to https://console.firebase.google.com
# 2. Create new project
# 3. Enable Firestore Database
# 4. Go to Project Settings → Service accounts
# 5. Generate new private key (JSON)
# 6. Extract projectId, client_email, and private_key from the JSON
# 7. Replace the values above with your Firebase credentials
