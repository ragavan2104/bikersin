# 🎯 VERCEL + SUPABASE DEPLOYMENT CONFIGURATION

# Database (Supabase PostgreSQL)
DATABASE_URL="postgresql://postgres:[RAGAVAn0204_]@db.yjaoxnairdqxsushflwg.supabase.co:5432/postgres"

# JWT Secret (Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET="your-super-secure-256-bit-jwt-secret-here"

# Node Environment
NODE_ENV=production

# CORS Origins (your Vercel frontend domains)
CORS_ORIGINS="https://bikers-tenant.vercel.app,https://bikers-superadmin.vercel.app"

# Optional: Rate Limiting with Upstash Redis (for production)
# UPSTASH_REDIS_REST_URL="your-upstash-redis-url"
# UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-token"

# Port (for local development, Vercel handles this automatically)
PORT=5000

# 📝 SETUP INSTRUCTIONS:
# 1. Create Supabase project at https://supabase.com/dashboard
# 2. Go to Settings → Database → Connection string
# 3. Copy the connection string and replace DATABASE_URL above
# 4. Generate JWT_SECRET using the command above
# 5. Update CORS_ORIGINS with your actual Vercel domain names